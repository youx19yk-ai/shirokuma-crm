services:
  - type: web
    name: shirokuma-crm
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # DATABASE_URLはRenderダッシュボードで手動設定（元crm-dbのInternal Database URLを使用）
